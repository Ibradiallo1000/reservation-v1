# Teliya — Rapport QA E2E public

Parcours statiquement validés : Marketplace vers résultats, compagnie, départs et booking ; accès direct `/:slug`, sous-domaine/domaine personnalisé via `RouteResolver`, puis même workflow réservation.

Tests automatisés : normalisation, déduplication, tri, texte libre refusé, partenaires éligibles, partenaire sans trajet, aliases et routes publiques.

Non exécuté faute de navigateur et de données staging autorisées : saisie réelle, PWA installée, domaine personnalisé réel, paiement, confirmation, billet, retour navigateur et refresh. Aucune revendication E2E réelle n’est faite.
