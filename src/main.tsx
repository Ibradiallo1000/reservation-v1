// src/main.tsx
// Petit "bootstrap" shim — garde ton index.tsx comme source principale.
// Vite attend /src/main.tsx depuis index.html ; on redirige vers index.tsx existant.

import "./index.tsx";
