rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ Tout le monde peut lire et écrire partout
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
