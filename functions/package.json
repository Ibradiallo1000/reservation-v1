{
  "name": "functions",
  "private": true,
  "engines": { "node": "18" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf lib",
    "deploy": "node -e \"throw new Error('Deploy Functions interdit sans projet explicite. Utilisez deploy:staging ou deploy:prod.')\"",
    "deploy:staging": "npm run build && firebase deploy --only functions --project teliya-staging",
    "deploy:prod": "npm run build && firebase deploy --only functions --project prod"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dayjs": "^1.11.10",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.4.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^18.19.0",
    "rimraf": "^5.0.5"
  }
}
